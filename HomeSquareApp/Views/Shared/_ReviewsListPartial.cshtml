@model IEnumerable<HomeSquareApp.Models.Review>

@if (Model.Count() > 0)
{
    foreach (Review review in Model)
    {
        <partial name="_reviewCardPartial" model="review" />
    }
}
else
{
    <p class="fst-italic p-5">
        Currently There Are No Review Regarding to The Product
    </p>
}